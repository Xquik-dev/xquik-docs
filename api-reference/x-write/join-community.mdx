---
title: "Join community"
description: "Join an existing X community with a connected account"
api: "POST /x/communities/{id}/join"
keywords: ["community", "join", "member", "subscribe"]
---

<Callout icon="coins" color="#5c3327">
  **5 credits per call** · [All plans](https://xquik.com/#pricing) from $0.00012/credit
</Callout>

<CodeGroup>
  ```bash cURL
  curl -X POST https://xquik.com/api/v1/x/communities/1893726451023847424/join \
    -H "x-api-key: xq_YOUR_KEY_HERE" \
    -H "Content-Type: application/json" \
    -d '{
      "account": "myxhandle"
    }' | jq
  ```

  ```javascript Node.js
  const response = await fetch(
    "https://xquik.com/api/v1/x/communities/1893726451023847424/join",
    {
      method: "POST",
      headers: {
        "x-api-key": "xq_YOUR_KEY_HERE",
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        account: "myxhandle",
      }),
    }
  );
  const data = await response.json();
  ```

  ```python Python
  import requests

  response = requests.post(
      "https://xquik.com/api/v1/x/communities/1893726451023847424/join",
      headers={"x-api-key": "xq_YOUR_KEY_HERE"},
      json={
          "account": "myxhandle",
      },
  )
  data = response.json()
  ```

  ```go Go
  package main

  import (
      "bytes"
      "encoding/json"
      "fmt"
      "net/http"
  )

  func main() {
      body, _ := json.Marshal(map[string]interface{}{
          "account": "myxhandle",
      })

      req, err := http.NewRequest("POST", "https://xquik.com/api/v1/x/communities/1893726451023847424/join", bytes.NewReader(body))
      if err != nil {
          panic(err)
      }
      req.Header.Set("x-api-key", "xq_YOUR_KEY_HERE")
      req.Header.Set("Content-Type", "application/json")

      resp, err := http.DefaultClient.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      var data map[string]interface{}
      if err := json.NewDecoder(resp.Body).Decode(&data); err != nil {
          panic(err)
      }
      fmt.Println(data)
  }
  ```
</CodeGroup>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported. Generate a key from the [dashboard](https://xquik.com/dashboard).
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`.
</ParamField>

## Path parameters

<ParamField path="id" type="string" required>
  The ID of the community to join.
</ParamField>

## Body

<ParamField body="account" type="string" required>
  The connected X account to join the community with. Must be a username you have connected to your Xquik account.
</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="communityId" type="string">The ID of the community joined.</ResponseField>
    <ResponseField name="communityName" type="string">The name of the community joined.</ResponseField>
    <ResponseField name="success" type="boolean">Always `true` on success.</ResponseField>

    ```json
    {
      "communityId": "1893726451023847424",
      "communityName": "Crypto Traders Hub",
      "success": true
    }
    ```
  </Tab>
  <Tab title="400 Invalid Input">
    ```json
    { "error": "invalid_input", "message": "Missing account field" }
    ```
    Missing `account` field in the request body.
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated", "message": "Missing or invalid API key" }
    ```
    Missing or invalid API key.
  </Tab>
  <Tab title="402 Subscription Required">
    ```json
    { "error": "no_subscription", "message": "No active subscription" }
    ```
    An active subscription is required. Subscribe from the [dashboard](https://xquik.com/subscription).
  </Tab>
  <Tab title="402 Usage Limit">
    ```json
    { "error": "usage_limit_reached", "message": "Monthly usage quota exceeded" }
    ```
    Your monthly usage quota has been exhausted. Usage resets at the start of each billing cycle.
  </Tab>
  <Tab title="404 Not Found">
    ```json
    { "error": "not_found", "message": "Community not found" }
    ```
    The specified community ID does not exist or is not publicly joinable.
  </Tab>
  <Tab title="409 Already Joined">
    ```json
    { "error": "already_member", "message": "Already a member of this community" }
    ```
    The specified account is already a member of this community.
  </Tab>
</Tabs>

<Note>
  **Related endpoints:** [Leave Community](/api-reference/x-write/leave-community) to leave a community, [Create Community](/api-reference/x-write/create-community) to create a new community, [Delete Community](/api-reference/x-write/delete-community) to remove a community you own.
</Note>
