---
title: "Send direct message"
description: "Send a direct message to an X user from one of your connected accounts"
api: "POST /x/dm/{userId}"
keywords: ["dm", "direct message", "send", "write", "connected account", "media"]
---

<Callout icon="coins" color="#5c3327">
  **2 credits per call** · $0.0003 on [PAYG](/guides/billing#pay-per-use-mpp)
</Callout>

<CodeGroup>
  ```bash cURL
  curl -X POST https://xquik.com/api/v1/x/dm/44196397 \
    -H "x-api-key: xq_YOUR_KEY_HERE" \
    -H "Content-Type: application/json" \
    -d '{
      "account": "myxaccount",
      "text": "Hello from Xquik!"
    }' | jq
  ```

  ```javascript Node.js
  const response = await fetch("https://xquik.com/api/v1/x/dm/44196397", {
    method: "POST",
    headers: {
      "x-api-key": "xq_YOUR_KEY_HERE",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      account: "myxaccount",
      text: "Hello from Xquik!",
    }),
  });
  const data = await response.json();
  ```

  ```python Python
  import requests

  response = requests.post(
      "https://xquik.com/api/v1/x/dm/44196397",
      headers={"x-api-key": "xq_YOUR_KEY_HERE"},
      json={
          "account": "myxaccount",
          "text": "Hello from Xquik!",
      },
  )
  data = response.json()
  ```

  ```go Go
  package main

  import (
      "bytes"
      "encoding/json"
      "fmt"
      "net/http"
  )

  func main() {
      body, _ := json.Marshal(map[string]interface{}{
          "account": "myxaccount",
          "text":    "Hello from Xquik!",
      })

      req, err := http.NewRequest("POST", "https://xquik.com/api/v1/x/dm/44196397", bytes.NewReader(body))
      if err != nil {
          panic(err)
      }
      req.Header.Set("x-api-key", "xq_YOUR_KEY_HERE")
      req.Header.Set("Content-Type", "application/json")

      resp, err := http.DefaultClient.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      var data map[string]interface{}
      if err := json.NewDecoder(resp.Body).Decode(&data); err != nil {
          panic(err)
      }
      fmt.Println(data)
  }
  ```
</CodeGroup>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported. Generate a key from the [dashboard](https://xquik.com/dashboard).
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`.
</ParamField>

## Path parameters

<ParamField path="userId" type="string" required>
  The X user ID of the recipient.
</ParamField>

## Body

<ParamField body="account" type="string" required>
  X username or account ID of your connected account to act as.
</ParamField>

<ParamField body="text" type="string" required>
  Message text to send. Maximum 10,000 characters.
</ParamField>

<ParamField body="media_ids" type="string[]">
  Array of media IDs to attach. Upload media first via the X media upload endpoint.
</ParamField>

<ParamField body="reply_to_message_id" type="string">
  ID of a previous message to reply to within the conversation.
</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="messageId" type="string">The ID of the sent message.</ResponseField>
    <ResponseField name="success" type="boolean">Always `true` on success.</ResponseField>

    ```json
    {
      "messageId": "1893726451029384192",
      "success": true
    }
    ```
  </Tab>
  <Tab title="400 Invalid Input">
    ```json
    { "error": "invalid_input", "message": "Missing or invalid text or account" }
    ```
    The request body is missing required fields or contains invalid values.
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated", "message": "Missing or invalid API key" }
    ```
    Missing or invalid API key.
  </Tab>
  <Tab title="402 Subscription Required">
    ```json
    { "error": "no_subscription", "message": "No active subscription" }
    ```
    An active subscription is required. Subscribe from the [dashboard](https://xquik.com/subscription).
  </Tab>
  <Tab title="402 Usage Limit">
    ```json
    { "error": "usage_limit_reached", "message": "Monthly usage limit reached" }
    ```
    Your monthly usage quota has been exhausted. Usage resets at the start of your next billing cycle.
  </Tab>
  <Tab title="502 Upstream Error">
    ```json
    { "error": "upstream_error", "message": "Failed to complete the action" }
    ```
    The request could not be completed upstream. Try again later.
  </Tab>
</Tabs>

<Note>
  **Related:** [Get User](/api-reference/x/get-user) to look up a user's ID before messaging, or [Follow User](/api-reference/x-write/follow) to follow the recipient.
</Note>
