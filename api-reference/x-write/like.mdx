---
title: "Like Tweet"
description: "Like a tweet from a connected X account"
api: "POST /x/tweets/{id}/like"
keywords: ["tweet", "like", "favorite", "engage", "x account"]
---

<Info>
  This is a metered endpoint. Each call deducts from your monthly usage quota.
</Info>

<CodeGroup>
  ```bash cURL
  curl -X POST https://xquik.com/api/v1/x/tweets/1895432178065391234/like \
    -H "x-api-key: xq_YOUR_KEY_HERE" \
    -H "Content-Type: application/json" \
    -d '{
      "account": "elonmusk"
    }' | jq
  ```

  ```javascript Node.js
  const tweetId = "1895432178065391234";
  const response = await fetch(`https://xquik.com/api/v1/x/tweets/${tweetId}/like`, {
    method: "POST",
    headers: {
      "x-api-key": "xq_YOUR_KEY_HERE",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      account: "elonmusk",
    }),
  });
  const data = await response.json();
  ```

  ```python Python
  import requests

  tweet_id = "1895432178065391234"
  response = requests.post(
      f"https://xquik.com/api/v1/x/tweets/{tweet_id}/like",
      headers={"x-api-key": "xq_YOUR_KEY_HERE"},
      json={
          "account": "elonmusk",
      },
  )
  data = response.json()
  ```

  ```go Go
  package main

  import (
      "bytes"
      "encoding/json"
      "fmt"
      "net/http"
  )

  func main() {
      tweetID := "1895432178065391234"
      body, _ := json.Marshal(map[string]interface{}{
          "account": "elonmusk",
      })

      req, err := http.NewRequest("POST", "https://xquik.com/api/v1/x/tweets/"+tweetID+"/like", bytes.NewReader(body))
      if err != nil {
          panic(err)
      }
      req.Header.Set("x-api-key", "xq_YOUR_KEY_HERE")
      req.Header.Set("Content-Type", "application/json")

      resp, err := http.DefaultClient.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      var data map[string]interface{}
      if err := json.NewDecoder(resp.Body).Decode(&data); err != nil {
          panic(err)
      }
      fmt.Println(data)
  }
  ```
</CodeGroup>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported. Generate a key from the [dashboard](https://xquik.com/dashboard).
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`.
</ParamField>

## Path Parameters

<ParamField path="id" type="string" required>
  The ID of the tweet to like.
</ParamField>

## Body

<ParamField body="account" type="string" required>
  X username or account ID identifying which connected X account will like the tweet. The `@` prefix is automatically stripped if included.
</ParamField>

## Response

<Tabs>
  <Tab title="200 Success">
    <ResponseField name="success" type="boolean">Always `true` on success.</ResponseField>

    ```json
    {
      "success": true
    }
    ```
  </Tab>
  <Tab title="400 Invalid Input">
    ```json
    { "error": "invalid_input", "message": "Invalid tweet ID or account" }
    ```
    Invalid tweet ID format or missing `account` field.
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated", "message": "Missing or invalid API key" }
    ```
    Missing or invalid API key.
  </Tab>
  <Tab title="402 Subscription Required">
    ```json
    { "error": "no_subscription", "message": "No active subscription" }
    ```
    An active subscription is required. Subscribe from the [dashboard](https://xquik.com/subscription).
  </Tab>
  <Tab title="402 Usage Limit">
    ```json
    { "error": "usage_limit_reached", "message": "Monthly usage limit reached" }
    ```
    Your monthly usage quota has been exhausted. Usage resets at the start of your next billing cycle.
  </Tab>
  <Tab title="502 Write Failed">
    ```json
    { "error": "x_write_failed", "message": "Failed to like tweet" }
    ```
    The tweet could not be liked. It may have been deleted, or the connected account may be rate-limited. Try again later.
  </Tab>
</Tabs>

<Note>
  **Related:** [Unlike Tweet](/api-reference/x-write/unlike) to remove a like, [Retweet](/api-reference/x-write/retweet) to retweet, or [Create Tweet](/api-reference/x-write/create-tweet) to post a new tweet.
</Note>
