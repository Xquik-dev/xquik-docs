---
title: "Top up credits"
description: "Get a Stripe checkout URL to purchase credits with a minimum of $10"
api: "POST /credits/topup"
keywords: ["credits", "top-up", "purchase", "billing", "stripe"]
---

<Callout icon="circle-check" color="#16a34a">
  **Free** — does not consume credits
</Callout>

<CodeGroup>
  ```bash cURL
  curl -X POST https://xquik.com/api/v1/credits/topup \
    -H "x-api-key: xq_YOUR_KEY_HERE" \
    -H "Content-Type: application/json" \
    -d '{}' | jq
  ```

  ```javascript Node.js
  const response = await fetch("https://xquik.com/api/v1/credits/topup", {
    method: "POST",
    headers: {
      "x-api-key": "xq_YOUR_KEY_HERE",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({}),
  });
  const data = await response.json();
  console.log(data);
  ```

  ```python Python
  import requests

  response = requests.post(
      "https://xquik.com/api/v1/credits/topup",
      headers={"x-api-key": "xq_YOUR_KEY_HERE"},
      json={},
  )
  data = response.json()
  print(data)
  ```
</CodeGroup>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported.
</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="url" type="string">Stripe checkout URL. Redirect the user to complete payment.</ResponseField>

    ```json
    {
      "url": "https://checkout.stripe.com/c/pay/cs_live_..."
    }
    ```
  </Tab>
  <Tab title="400 Invalid input">
    ```json
    { "error": "Minimum top-up is $10" }
    ```
    The top-up amount is below the $10 minimum.
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated" }
    ```
    Missing or invalid API key.
  </Tab>
</Tabs>
