---
title: "Create API Key"
description: "Generate a new API key for programmatic access to the Xquik API."
---

<CodeGroup>
  ```bash cURL
  curl -X POST https://xquik.com/api/v1/api-keys \
    -H "Cookie: session=YOUR_SESSION_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"name": "Production"}' | jq
  ```

  ```javascript Node.js
  const response = await fetch("https://xquik.com/api/v1/api-keys", {
    method: "POST",
    headers: {
      "Cookie": "session=YOUR_SESSION_TOKEN",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ name: "Production" }),
  });
  const key = await response.json();
  console.log(key);
  ```

  ```python Python
  import requests

  response = requests.post(
      "https://xquik.com/api/v1/api-keys",
      cookies={"session": "YOUR_SESSION_TOKEN"},
      json={"name": "Production"},
  )
  key = response.json()
  print(key)
  ```

  ```go Go
  package main

  import (
  	"bytes"
  	"encoding/json"
  	"fmt"
  	"io"
  	"log"
  	"net/http"
  )

  func main() {
  	body, err := json.Marshal(map[string]string{"name": "Production"})
  	if err != nil {
  		log.Fatal(err)
  	}

  	req, err := http.NewRequest("POST", "https://xquik.com/api/v1/api-keys", bytes.NewReader(body))
  	if err != nil {
  		log.Fatal(err)
  	}
  	req.Header.Set("Content-Type", "application/json")
  	req.AddCookie(&http.Cookie{Name: "session", Value: "YOUR_SESSION_TOKEN"})

  	resp, err := http.DefaultClient.Do(req)
  	if err != nil {
  		log.Fatal(err)
  	}
  	defer resp.Body.Close()

  	respBody, err := io.ReadAll(resp.Body)
  	if err != nil {
  		log.Fatal(err)
  	}
  	fmt.Println(string(respBody))
  }
  ```
</CodeGroup>

## Headers

<ParamField header="Cookie" type="string" required>
  Session cookie from the dashboard. Format: `session=YOUR_SESSION_TOKEN`.
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`.
</ParamField>

## Body

<ParamField body="name" type="string">
  Display name for the key. Defaults to `"Default"` if omitted.
</ParamField>

## Response

<Tabs>
  <Tab title="201 Created">
    <ResponseField name="id" type="string">
      Unique identifier for the API key.
    </ResponseField>
    <ResponseField name="fullKey" type="string">
      The complete API key including the `xqk_` prefix.
    </ResponseField>
    <ResponseField name="prefix" type="string">
      First 4 characters after `xqk_` for identification.
    </ResponseField>
    <ResponseField name="name" type="string">
      Display name of the key.
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      ISO 8601 creation timestamp.
    </ResponseField>

    ```json
    {
      "id": "42",
      "fullKey": "xqk_a1b2c3d4e5f6789012345678901234",
      "prefix": "a1b2",
      "name": "Production",
      "createdAt": "2026-02-24T10:30:00.000Z"
    }
    ```

    <Warning>
      The `fullKey` is returned **only once**. Store it securely -- it cannot be retrieved again.
    </Warning>
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated" }
    ```
    Missing or invalid session cookie. Sign in to the dashboard first.
  </Tab>
</Tabs>

<Note>
  This endpoint requires **session cookie** authentication (dashboard sign-in). API keys cannot create other API keys.

  **Related:** [List API Keys](/api-reference/api-keys/list) &middot; [Revoke API Key](/api-reference/api-keys/revoke)
</Note>
