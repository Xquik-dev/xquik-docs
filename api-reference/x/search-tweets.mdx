---
title: "Search Tweets"
description: "Search tweets by query string."
---

<Warning>
  This endpoint is metered. Each call counts against your monthly usage quota. Cost scales with the number of tweets returned. Check your remaining balance via [Get Account](/api-reference/account/get).
</Warning>

<CodeGroup>
  ```bash cURL
  curl -G https://xquik.com/api/v1/x/tweets/search \
    --data-urlencode "q=from:xquikcom giveaway" \
    -H "x-api-key: xq_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2" | jq
  ```

  ```javascript Node.js
  const query = encodeURIComponent("from:xquikcom giveaway");
  const response = await fetch(`https://xquik.com/api/v1/x/tweets/search?q=${query}`, {
    headers: { "x-api-key": "xq_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2" },
  });
  const data = await response.json();
  console.log(data);
  ```

  ```python Python
  import requests

  response = requests.get(
      "https://xquik.com/api/v1/x/tweets/search",
      params={"q": "from:xquikcom giveaway"},
      headers={"x-api-key": "xq_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2"},
  )
  data = response.json()
  print(data)
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"io"
  	"log"
  	"net/http"
  	"net/url"
  )

  func main() {
  	baseURL := "https://xquik.com/api/v1/x/tweets/search"
  	params := url.Values{}
  	params.Set("q", "from:xquikcom giveaway")

  	req, err := http.NewRequest("GET", baseURL+"?"+params.Encode(), nil)
  	if err != nil {
  		log.Fatal(err)
  	}
  	req.Header.Set("x-api-key", "xq_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2")

  	resp, err := http.DefaultClient.Do(req)
  	if err != nil {
  		log.Fatal(err)
  	}
  	defer resp.Body.Close()

  	body, err := io.ReadAll(resp.Body)
  	if err != nil {
  		log.Fatal(err)
  	}
  	fmt.Println(string(body))
  }
  ```
</CodeGroup>

## Query parameters

<ParamField query="q" type="string" required>
  Search query. Supports X search operators such as `from:username`, `to:username`, `#hashtag`, and boolean operators.
</ParamField>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported.
</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="tweets" type="object[]">
      Array of matching tweets.
      <Expandable title="tweet object">
        <ResponseField name="id" type="string">Tweet ID.</ResponseField>
        <ResponseField name="text" type="string">Tweet text content.</ResponseField>
        <ResponseField name="createdAt" type="string">ISO 8601 creation timestamp. Omitted if unavailable.</ResponseField>
        <ResponseField name="likeCount" type="number">Like count. Omitted if unavailable.</ResponseField>
        <ResponseField name="retweetCount" type="number">Retweet count. Omitted if unavailable.</ResponseField>
        <ResponseField name="replyCount" type="number">Reply count. Omitted if unavailable.</ResponseField>
        <ResponseField name="author" type="object">
          Tweet author. Omitted if author data is unavailable.
          <Expandable title="author object">
            <ResponseField name="id" type="string">Author user ID.</ResponseField>
            <ResponseField name="username" type="string">Author X username.</ResponseField>
            <ResponseField name="name" type="string">Author display name.</ResponseField>
            <ResponseField name="verified" type="boolean">Whether the author is verified. Omitted if unavailable.</ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="total" type="number">
      Number of tweets returned in this response.
    </ResponseField>

    ```json
    {
      "tweets": [
        {
          "id": "1893456789012345678",
          "text": "Tweet content here",
          "createdAt": "2026-02-24T10:00:00.000Z",
          "likeCount": 150,
          "retweetCount": 42,
          "replyCount": 10,
          "author": {
            "id": "987654321",
            "username": "xquikcom",
            "name": "Xquik",
            "verified": true
          }
        }
      ],
      "total": 1
    }
    ```
  </Tab>
  <Tab title="400 Missing query">
    ```json
    { "error": "missing_query" }
    ```
    The `q` query parameter is empty or missing.
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated" }
    ```
    Missing or invalid API key. Check the `x-api-key` header value.
  </Tab>
  <Tab title="402 Insufficient credits">
    ```json
    { "error": "insufficient_credits" }
    ```
    Monthly usage quota exceeded. Wait for the next billing period or check your balance via [Get Account](/api-reference/account/get).
  </Tab>
  <Tab title="502 X API unavailable">
    ```json
    { "error": "x_api_unavailable" }
    ```
    The upstream X API returned an error. Retry after a short delay.
  </Tab>
</Tabs>

<Note>
  **Next steps:** [Get Tweet](/api-reference/x/get-tweet) to fetch full details for a specific tweet, or [Get User](/api-reference/x/get-user) to look up an author profile.
</Note>
