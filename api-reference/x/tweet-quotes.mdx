---
title: "Get quote tweets"
description: "Retrieve a paginated list of tweets that quote a specific tweet by its ID"
api: "GET /x/tweets/{id}/quotes"
keywords: ["quotes", "quote tweets", "retweets", "x api"]
---

<Callout icon="coins" color="#5c3327">
  **1 credit per tweet returned** · [All plans](https://xquik.com/#pricing) from $0.00012/credit
</Callout>

<CodeGroup>
  ```bash cURL
  curl "https://xquik.com/api/v1/x/tweets/1893456789012345678/quotes" \
    -H "x-api-key: xq_YOUR_KEY_HERE" | jq
  ```

  ```javascript Node.js
  const tweetId = "1893456789012345678";
  const response = await fetch(`https://xquik.com/api/v1/x/tweets/${tweetId}/quotes`, {
    headers: { "x-api-key": "xq_YOUR_KEY_HERE" },
  });
  const data = await response.json();
  console.log(data);
  ```

  ```python Python
  import requests

  response = requests.get(
      "https://xquik.com/api/v1/x/tweets/1893456789012345678/quotes",
      headers={"x-api-key": "xq_YOUR_KEY_HERE"},
  )
  data = response.json()
  print(data)
  ```
</CodeGroup>

## Path parameters

<ParamField path="id" type="string" required>
  Tweet ID (numeric string).
</ParamField>

## Query parameters

<ParamField query="cursor" type="string">
  Pagination cursor from a previous response. Omit for the first page.
</ParamField>

<ParamField query="sinceTime" type="string">
  ISO 8601 timestamp. Only return quotes after this time.
</ParamField>

<ParamField query="untilTime" type="string">
  ISO 8601 timestamp. Only return quotes before this time.
</ParamField>

<ParamField query="includeReplies" type="boolean">
  Include reply tweets. Default: `false`.
</ParamField>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported.
</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="tweets" type="object[]">Array of quote tweets.</ResponseField>
    <ResponseField name="has_next_page" type="boolean">Whether more results are available.</ResponseField>
    <ResponseField name="next_cursor" type="string">Cursor for the next page.</ResponseField>

    ```json
    {
      "tweets": [
        {
          "id": "1893456789012345679",
          "text": "Great point! RT @user: ...",
          "createdAt": "2026-03-27T11:00:00.000Z",
          "likeCount": 10
        }
      ],
      "has_next_page": true,
      "next_cursor": "DAACCgACGE..."
    }
    ```
  </Tab>
  <Tab title="400 Invalid tweet ID">
    ```json
    { "error": "invalid_tweet_id" }
    ```
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated" }
    ```
  </Tab>
  <Tab title="402 Subscription required">
    ```json
    { "error": "no_subscription" }
    ```
  </Tab>
</Tabs>
