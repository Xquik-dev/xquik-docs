---
title: "Get DM history"
description: "Retrieve direct message history with a specific X user from the authenticated account"
api: "GET /x/dm/{id}/history"
keywords: ["direct messages", "dm", "chat history", "x api"]
---

<Callout icon="coins" color="#5c3327">
  **1 credit per result returned** · [All plans](https://xquik.com/#pricing) from $0.00012/credit
</Callout>

<Note>
  Requires a connected X account passed via the `account` query parameter. The account must be a participant in the conversation - DM reads cannot be routed through the shared read pool.
</Note>

<CodeGroup>
  ```bash cURL
  curl -G https://xquik.com/api/v1/x/dm/44196397/history \
    --data-urlencode "account=your_handle" \
    -H "x-api-key: xq_YOUR_KEY_HERE" | jq

  # Page 2
  curl -G https://xquik.com/api/v1/x/dm/44196397/history \
    --data-urlencode "account=your_handle" \
    --data-urlencode "cursor=abc123" \
    -H "x-api-key: xq_YOUR_KEY_HERE" | jq
  ```

  ```javascript Node.js
  const userId = "44196397";
  const account = "your_handle";
  const response = await fetch(
    `https://xquik.com/api/v1/x/dm/${userId}/history?account=${account}`,
    { headers: { "x-api-key": "xq_YOUR_KEY_HERE" } },
  );
  const data = await response.json();
  console.log(data.messages);

  // Paginate
  if (data.has_next_page) {
    const next = await fetch(
      `https://xquik.com/api/v1/x/dm/${userId}/history?account=${account}&cursor=${data.next_cursor}`,
      { headers: { "x-api-key": "xq_YOUR_KEY_HERE" } },
    );
    console.log((await next.json()).messages);
  }
  ```

  ```python Python
  import requests

  response = requests.get(
      "https://xquik.com/api/v1/x/dm/44196397/history",
      params={"account": "your_handle"},
      headers={"x-api-key": "xq_YOUR_KEY_HERE"},
  )
  data = response.json()
  print(data["messages"])

  # Paginate
  while data["has_next_page"]:
      data = requests.get(
          "https://xquik.com/api/v1/x/dm/44196397/history",
          params={"account": "your_handle", "cursor": data["next_cursor"]},
          headers={"x-api-key": "xq_YOUR_KEY_HERE"},
      ).json()
      print(data["messages"])
  ```
</CodeGroup>

## Path parameters

<ParamField path="id" type="string" required>
  Target X user ID for the DM conversation (numeric string).
</ParamField>

## Query parameters

<ParamField query="account" type="string" required>
  X handle (without the `@` prefix) of the connected X account whose session reads the conversation. DM history is participant-scoped on the X protocol level, so the account must be a participant in the conversation. Connect an account on the dashboard before calling this endpoint.
</ParamField>

<ParamField query="cursor" type="string">
  Pagination cursor. Pass the `next_cursor` value from the previous response to fetch older messages.
</ParamField>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported.
</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="messages" type="object[]">
      Array of direct messages.
      <Expandable title="message object">
        <ResponseField name="id" type="string">Message ID.</ResponseField>
        <ResponseField name="text" type="string">Message text. Omitted if unavailable.</ResponseField>
        <ResponseField name="senderId" type="string">Sender user ID. Omitted if unavailable.</ResponseField>
        <ResponseField name="receiverId" type="string">Receiver user ID. Omitted if unavailable.</ResponseField>
        <ResponseField name="createdAt" type="string">ISO 8601 timestamp. Omitted if unavailable.</ResponseField>
        <ResponseField name="mediaUrl" type="string">URL of attached media (image, GIF, or video). Omitted when the message has no media attachment.</ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="has_next_page" type="boolean">
      Whether older messages are available.
    </ResponseField>
    <ResponseField name="next_cursor" type="string">
      Opaque cursor for the next page. Empty string when no more messages.
    </ResponseField>

    ```json
    {
      "messages": [
        {
          "id": "1893456789012345678",
          "text": "Hey, great tool!",
          "senderId": "44196397",
          "receiverId": "987654321",
          "createdAt": "2026-02-24T10:00:00.000Z"
        }
      ],
      "has_next_page": true,
      "next_cursor": "1893456789012345677"
    }
    ```
  </Tab>
  <Tab title="400 Invalid user ID">
    ```json
    { "error": "invalid_user_id" }
    ```
    The user ID is empty or invalid.
  </Tab>
  <Tab title="400 Account required">
    ```json
    { "error": "account_required", "message": "Provide an account query parameter naming a connected X account. DM history requires a participant session." }
    ```
    The `account` query parameter was missing or empty. Pass the handle of a connected X account that participates in the conversation.
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated" }
    ```
    Missing or invalid API key.
  </Tab>
  <Tab title="402 Subscription required">
    ```json
    { "error": "no_subscription" }
    ```
    No active subscription or insufficient credits. Possible error values: `no_subscription`, `subscription_inactive`, `no_credits`, `insufficient_credits`.
  </Tab>
  <Tab title="403 DM not permitted">
    ```json
    { "error": "dm_not_permitted", "message": "X rejected the DM read. The connected account is not a participant in this conversation, or its session expired. Reconnect the account on the dashboard and try again." }
    ```
    X rejected the DM read. The connected account is not a participant in this conversation, or its session expired. Reconnect the account and retry.
  </Tab>
  <Tab title="403 Account restricted">
    ```json
    { "error": "account_restricted" }
    ```
    The connected X account is suspended, locked, or otherwise restricted. Use a different connected account.
  </Tab>
  <Tab title="403 Account needs reauth">
    ```json
    { "error": "account_needs_reauth" }
    ```
    The connected account needs to be reauthenticated. Reconnect it from the dashboard.
  </Tab>
  <Tab title="502 X API unavailable">
    ```json
    { "error": "x_api_unavailable" }
    ```
    The X data source returned an error. Retry after a short delay.
  </Tab>
</Tabs>

<Note>
  **Related:** [Send DM](/api-reference/x-write/send-dm) · [Notifications](/api-reference/x/notifications)
</Note>
