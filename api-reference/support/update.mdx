---
title: "Xquik support ticket API to update request status"
sidebarTitle: "Update ticket status"
description: "Resolve, close, or reopen a support ticket by ID. Its messages and attachments stay unchanged. Support staff alone set in_progress. The call costs no credits."
api: "PATCH /support/tickets/{id}"
keywords: ["support", "ticket", "update", "status"]
---

import LlmsDirective from "/snippets/llms-directive.mdx";

{/* GENERATED RESPONSE EXAMPLES START */}

<Panel>

<Tabs defaultTabIndex={0} sync={false}>

  <Tab title="200" id="response-support-update-200">

    ```json
    {
      "publicId": "tkt_a1b2c3d4e5f6a1b2c3d4e5f6",
      "status": "resolved"
    }
    ```

  </Tab>

  <Tab title="400" id="response-support-update-400">

    ```json
    {
      "error": "invalid_input",
      "message": "Invalid input. Check the request body."
    }
    ```

  </Tab>

  <Tab title="401" id="response-support-update-401">

    ```json
    {
      "error": "unauthenticated",
      "message": "Authentication required. Provide a valid API key or bearer token."
    }
    ```

  </Tab>

  <Tab title="404" id="response-support-update-404">

    ```json
    {
      "error": "not_found",
      "message": "Resource not found."
    }
    ```

  </Tab>

  <Tab title="429" id="response-support-update-429">

    ```json
    {
      "error": "rate_limit_exceeded",
      "message": "Too many requests. Try again later.",
      "retryAfter": 60
    }
    ```

  </Tab>
</Tabs>

</Panel>
{/* GENERATED RESPONSE EXAMPLES END */}
<LlmsDirective />

<Callout icon="circle-check" color="#16a34a">

**Free.** This endpoint does not consume credits.

</Callout>

<CodeGroup>

```bash cURL
curl -X PATCH https://xquik.com/api/v1/support/tickets/tkt_a1b2c3d4e5f6a1b2c3d4e5f6 \
  -H "x-api-key: xq_YOUR_KEY_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "resolved"
  }' | jq
```

```javascript Node.js
const ticketId = "tkt_a1b2c3d4e5f6a1b2c3d4e5f6";
const response = await fetch(`https://xquik.com/api/v1/support/tickets/${ticketId}`, {
  method: "PATCH",
  headers: {
    "x-api-key": "xq_YOUR_KEY_HERE",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    status: "resolved",
  }),
});
const data = await response.json();
```

```python Python
import requests

ticket_id = "tkt_a1b2c3d4e5f6a1b2c3d4e5f6"
response = requests.patch(
    f"https://xquik.com/api/v1/support/tickets/{ticket_id}",
    headers={"x-api-key": "xq_YOUR_KEY_HERE"},
    json={"status": "resolved"},
)
data = response.json()
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    ticketID := "tkt_a1b2c3d4e5f6a1b2c3d4e5f6"
    body, _ := json.Marshal(map[string]interface{}{
        "status": "resolved",
    })

    req, err := http.NewRequest("PATCH", "https://xquik.com/api/v1/support/tickets/"+ticketID, bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("x-api-key", "xq_YOUR_KEY_HERE")
    req.Header.Set("Content-Type", "application/json")

    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    var data map[string]interface{}
    if err := json.NewDecoder(resp.Body).Decode(&data); err != nil {
        panic(err)
    }
    fmt.Println(data)
}
```

</CodeGroup>

## Path parameters

<ParamField path="id" type="string" required>

The ticket public ID (for example `tkt_a1b2c3d4e5f6a1b2c3d4e5f6`). Returned when you [create a ticket](/api-reference/support/create) or [list tickets](/api-reference/support/list).

</ParamField>

## Headers

<ParamField header="x-api-key" type="string" required>

Your API key. Session cookie authentication is also supported. Generate a key from the [dashboard](https://xquik.com/dashboard).

</ParamField>

<ParamField header="Content-Type" type="string" required>

Must be `application/json`.

</ParamField>

## Body

<ParamField body="status" type="string" required>

New status. One of: `open`, `resolved`, `closed`. Support staff set the `in_progress` status. You cannot set it through the API.

</ParamField>

## Response

### 200 OK

<ResponseField name="publicId" type="string">
  Unique ticket public ID.
</ResponseField>
<ResponseField name="status" type="string">
  Updated ticket status.
</ResponseField>

```json
{
  "publicId": "tkt_a1b2c3d4e5f6a1b2c3d4e5f6",
  "status": "resolved"
}
```

### 400 Invalid input

```json
{ "error": "invalid_input", "message": "Invalid input. Check the request body." }
```

Missing or invalid `status` value.

### 401 Unauthenticated

```json
{
  "error": "unauthenticated",
  "message": "Authentication required. Provide a valid API key or bearer token."
}
```

Missing or invalid API key.

### 404 Not found

```json
{ "error": "not_found", "message": "Ticket not found." }
```

No support ticket matches the supplied public ID.

### 429 Rate limited

```json
{
  "error": "rate_limit_exceeded",
  "message": "Too many requests. Try again later.",
  "retryAfter": 60
}
```

Wait for the `Retry-After` value before updating ticket status again.

<Note>

**Related.** [Get Ticket](/api-reference/support/get) to fetch ticket details and message history, or [Reply to Ticket](/api-reference/support/reply) to add a message.

</Note>
