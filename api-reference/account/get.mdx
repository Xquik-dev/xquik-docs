---
title: "Get Account"
description: "Retrieve account info, subscription status, and current period usage."
---

<CodeGroup>
  ```bash cURL
  curl https://xquik.com/api/v1/account \
    -H "x-api-key: xqk_a1b2c3d4e5f6789012345678901234" | jq
  ```

  ```javascript Node.js
  const response = await fetch("https://xquik.com/api/v1/account", {
    headers: { "x-api-key": "xqk_a1b2c3d4e5f6789012345678901234" },
  });
  const account = await response.json();
  console.log(account);
  ```

  ```python Python
  import requests

  response = requests.get(
      "https://xquik.com/api/v1/account",
      headers={"x-api-key": "xqk_a1b2c3d4e5f6789012345678901234"},
  )
  account = response.json()
  print(account)
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"io"
  	"log"
  	"net/http"
  )

  func main() {
  	req, err := http.NewRequest("GET", "https://xquik.com/api/v1/account", nil)
  	if err != nil {
  		log.Fatal(err)
  	}
  	req.Header.Set("x-api-key", "xqk_a1b2c3d4e5f6789012345678901234")

  	resp, err := http.DefaultClient.Do(req)
  	if err != nil {
  		log.Fatal(err)
  	}
  	defer resp.Body.Close()

  	body, err := io.ReadAll(resp.Body)
  	if err != nil {
  		log.Fatal(err)
  	}
  	fmt.Println(string(body))
  }
  ```
</CodeGroup>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Generate one from the [dashboard](/api-reference/api-keys/create).
</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="plan" type="string">
      Subscription status. `"active"` or `"inactive"`.
    </ResponseField>
    <ResponseField name="monitorsAllowed" type="number">
      Maximum monitors your plan allows.
    </ResponseField>
    <ResponseField name="monitorsUsed" type="number">
      Number of currently active monitors.
    </ResponseField>
    <ResponseField name="currentPeriod" type="object">
      Current billing period. Omitted if no active subscription.
      <Expandable title="currentPeriod object">
        <ResponseField name="start" type="string">
          ISO 8601 period start timestamp.
        </ResponseField>
        <ResponseField name="end" type="string">
          ISO 8601 period end timestamp.
        </ResponseField>
        <ResponseField name="capMicrocents" type="string">
          Monthly usage cap in microcents. $10 = `"10000000000"`.
        </ResponseField>
        <ResponseField name="usedMicrocents" type="string">
          Usage consumed this period in microcents.
        </ResponseField>
      </Expandable>
    </ResponseField>

    ```json
    {
      "plan": "active",
      "monitorsAllowed": 1,
      "monitorsUsed": 0,
      "currentPeriod": {
        "start": "2026-02-01T00:00:00.000Z",
        "end": "2026-03-01T00:00:00.000Z",
        "capMicrocents": "10000000000",
        "usedMicrocents": "1500000"
      }
    }
    ```
  </Tab>
  <Tab title="401 Unauthenticated">
    ```json
    { "error": "unauthenticated" }
    ```
    Missing or invalid API key. Check the `x-api-key` header value.
  </Tab>
</Tabs>

## Usage Cap

Every subscriber has a **$10/month usage cap** ($10 = 10,000,000,000 microcents). Operations are metered automatically and tracked in the `currentPeriod.usedMicrocents` field.

| Operation | Cost (microcents) |
|---|---|
| Tweet search | 150 |
| User lookup | 180 |
| Follower fetch | 150 |
| Verified follower fetch | 300 |
| Follow check | 150 |

Monitor the `usedMicrocents` / `capMicrocents` ratio to avoid hitting the cap. When the cap is reached, API requests that consume usage will return `402`.

<Note>
  **Related:** [Authentication](/api-reference/authentication) &middot; [Billing & Usage](/quickstart#billing)
</Note>
