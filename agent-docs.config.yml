url: https://docs.xquik.com
# content-start-position stays disabled until the bounded afdocs sample starts
# main content before generated Mintlify navigation on every sampled page.
# Keep the live CI crawl deterministic and bounded so transient network latency
# does not hide repo guard failures behind a hook timeout.
# llms-txt-directive-html stays enabled even when it warns on buried positions:
# samples confirm directives exist, but generated Mintlify navigation can push
# page-body content past 50% on API pages. Treat that warning as generated HTML
# layout risk, not missing page snippets.
checks:
  - llms-txt-exists
  - llms-txt-valid
  - llms-txt-size
  - llms-txt-links-resolve
  - llms-txt-links-markdown
  - llms-txt-directive-html
  - llms-txt-directive-md
  - markdown-url-support
  - content-negotiation
  - rendering-strategy
  - page-size-html
  - page-size-markdown
  - tabbed-content-serialization
  - section-header-quality
  - markdown-code-fence-validity
  - http-status-codes
  - redirect-behavior
  - llms-txt-coverage
  - markdown-content-parity
  - cache-header-hygiene
  - auth-gate-detection
  - auth-alternative-access
options:
  maxConcurrency: 32
  maxLinksToTest: 30
  requestDelay: 0
  requestTimeout: 10000
  samplingStrategy: deterministic
